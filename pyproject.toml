[project]
name = "claude-history"
version = "0.2.0"
description = "Web-based conversation history browser powered by Claude Archive"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.3",
    "gunicorn>=22.0",
    "python-docx>=1.2.0",
    "google-cloud-storage>=2.18.0",
    "claude-archive",
]

[tool.uv.sources]
claude-archive = { path = "../Claude-Archive" }
